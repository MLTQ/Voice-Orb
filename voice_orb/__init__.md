# __init__.py

## Purpose
Defines the package version for `Voice-Orb`.

## Components

### `__version__`
- **Does**: Exposes the package version used by the runtime plugin handshake.
- **Interacts with**: `voice_orb/server.py`.
